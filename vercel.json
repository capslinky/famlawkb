{
  "outputDirectory": "site",
  "buildCommand": "echo 'Using pre-built site'",
  "installCommand": "echo 'No install needed'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/auth"
    }
  ]
}